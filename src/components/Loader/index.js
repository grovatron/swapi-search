import React from 'react';
import loaderSrc from '../../assets/loader.gif';

const Loader = () => (
  <div>
    <img
      alt='loader'
      src={loaderSrc}
      width='128px'
    />
  </div>
);

export default Loader;
